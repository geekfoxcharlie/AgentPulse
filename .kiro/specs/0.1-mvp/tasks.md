# AgentPulse 0.1 MVP Tasks

Spec version: 0.1  
Status: Ready for local configuration

任务按顺序执行。完成任务时直接更新当前实现；0.1 不创建兼容版本或平行实现。

## 1. 确定实现基线

- [x] 选择 CLI 与 Web 的语言、运行时和最低版本。
- [x] 选择配置 Schema 校验、健康缓存和测试工具。
- [x] 将技术选择、理由和目标代码目录补充到 `design.md`。
- [x] 建立最小构建、类型检查和测试命令。
- [x] 确保一次安装同时提供 `agentpulse` CLI 和本地 Web 能力。

## 2. 建立配置契约

- [x] 定义能力组机器可读 Schema。
- [x] 定义 API 机器可读 Schema。
- [x] 定义带版本的 CLI JSON 输出 Schema。
- [x] 建立用户级配置和状态目录解析规则。
- [x] 实现完整注册表校验，包括重复 ID 和未知能力组。
- [x] 提供不含真实密钥的示例能力组与 API 配置。

## 3. 接入首批搜索模板

- [x] 提供 `search` 能力组内置定义。
- [x] 根据官方当前文档实现 Brave Search 模板和最小探测。
- [x] 根据官方当前文档实现 Tavily Search 模板和最小探测。
- [x] 根据官方当前文档实现 Exa Search 模板和最小探测。
- [x] 根据官方当前文档实现 Firecrawl Search 模板和最小探测。
- [x] 根据官方当前文档实现 Serper Google Search 模板和最小探测。
- [x] 根据官方当前文档实现 X API Search Posts 模板和最小探测。
- [x] 移除不属于当前独立搜索目录的 SerpAPI Google Search 模板，并排除模型服务端搜索工具。
- [x] 在 Brave 模板中提示 Agent 先检查本机健康快照。
- [x] 实现按能力组列出模板的 CLI 查询。
- [x] 实现由模板实例化完整用户配置的 CLI 命令。
- [x] 确认模板不包含密钥、不直接参与运行时查询，更新模板不改变已有用户配置。

## 4. 实现共享核心

- [x] 实现配置加载与统一查询模型。
- [x] 实现配置原子写入。
- [x] 实现环境变量存在性检查和凭据脱敏工具。
- [x] 实现配置变更检测和缓存失效接口。
- [x] 确保 Web 与 CLI 只能通过共享核心取得业务数据。

## 5. 实现 Agent 查询和配置 CLI

- [x] 实现能力组列表查询。
- [x] 实现单个能力组和组内 API 查询。
- [x] 实现单个 API 详情查询。
- [x] 为全部查询实现版本化 JSON 输出。
- [x] 实现能力组新增和更新命令。
- [x] 实现 API 新增、更新、启用和停用命令。
- [x] 实现全量配置校验命令。
- [x] 为未知 ID 和无效输入实现结构化错误及非零退出码。

## 6. 实现健康检查

- [x] 实现每个 API 的最小探测执行器。
- [x] 支持 GET、POST、Query、JSON Body、Header/Bearer/Query 凭据注入和响应字段校验。
- [x] 实现一小时默认 TTL 和配置指纹。
- [x] 实现用户级健康缓存的原子读写。
- [x] 实现按组并行探测和单项故障隔离。
- [x] 实现同一探测的进程内并发去重机制。
- [x] 实现五种健康状态和脱敏错误类别。
- [x] 将带健康参数的组查询接入缓存和探测逻辑。

## 7. 实现只读 Web

- [x] 实现能力组概览。
- [x] 实现能力组 API 列表。
- [x] 实现 API 详情、凭据引用和调用说明展示。
- [x] 展示未知、过期和最近健康状态。
- [x] 默认只监听回环地址。
- [x] 确认页面不存在配置写入口。
- [x] 确认页面加载和刷新不触发健康检查。
- [x] 不发布 Web 内部路由作为正式 HTTP API。

## 8. 提供 Agent 使用入口

- [x] 编写可注入 Agent 上下文的短引导文本。
- [x] 在引导中使用最终 CLI 命令和 JSON 参数。
- [x] 验证 CLI 只依赖用户级配置而不依赖项目目录。
- [x] 验证 Agent 能从查询结果直接构造第三方 API 请求。
- [x] 验证 Agent 能区分可安装模板和已经配置的 API。

## 9. 自动化验证

- [x] 覆盖配置 Schema、交叉引用和重复 ID。
- [x] 覆盖六个搜索模板的请求方法、端点、认证位置和最小参数。
- [x] 覆盖模板实例化及模板更新不改变已有配置。
- [x] 覆盖 CLI JSON 版本、输出和错误退出码。
- [x] 覆盖 TTL 命中、过期和配置变更失效。
- [x] 覆盖探测并发和进程内去重。
- [x] 使用测试密钥验证所有输出和错误均已脱敏。
- [x] 覆盖 Web 只读边界和页面读取不触发探测。
- [x] 验证 CLI 在 Web 未运行时独立工作。
- [x] 运行类型检查、测试和构建命令。

## 10. 更新文档并关闭 Spec

- [x] 用最终实现命令更新 README 的快速入口，但不复制需求正文。
- [x] 将已确定的当前架构同步到 `docs/ARCHITECTURE.md`。
- [x] 若产品范围发生变化，更新 `docs/REQUIREMENTS.md` 的唯一对应条目。
- [x] 检查根目录没有新增其他主文档。
- [x] 检查 README、长期文档、Schema 和 Spec 没有竞争性事实。
- [ ] 取得用户的真实 API 配置并完成首轮真实健康检查后，再关闭 Spec。

## 11. 等待本机配置

以下步骤需要当前用户提供已经申请好的真实 API Key 或其已存在的位置，因此不由仓库代码代替：

- [ ] 把希望启用的搜索 API Key 写入本机环境变量或已登记的凭据文件。
- [ ] 使用 `agentpulse api add --template <template-id> --configured-at <path>` 实例化希望使用的模板。
- [ ] 在新的终端会话中执行 `agentpulse group search --health --json`，核对真实探测结果。
- [ ] 把符合预期的健康快照保留一小时，并确认同一查询不重复消耗额度。

## 12. 完成定义

0.1 在 CLI 配置与查询、按组健康检查、只读 Web、Agent 引导和自动化验证形成完整闭环后完成。任何未实现的必要功能都不能仅通过文档说明而视为完成。
