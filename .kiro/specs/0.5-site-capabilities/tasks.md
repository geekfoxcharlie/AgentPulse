# AgentPulse 0.5 Site Capabilities Tasks

- [x] Add `site` schema, types, config load/validate, templates, CLI, query views, and web render.
- [x] Add the `sites` group and thirteen built-in site templates.
- [x] Skip sites in health probes; list them as `unknown`/`disabled`.
- [x] Tests for catalog, materialization, ID collision, health skip, CLI, and web.
- [x] Update REQUIREMENTS, ARCHITECTURE, README, agent-context, and InfoStack routing.
